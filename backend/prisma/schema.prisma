generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum CharityTab {
  ORG
  CAMPAIGN
  MERCHANDISE
}

model Charity {
  id           String     @id @default(cuid())
  title        String
  description  String
  tab          CharityTab
  categoryCode String
  logoUrl      String?
  /// 單位：NTD 整數（元）
  amountRaised Int        @default(0)
  /// 單位：NTD 整數（元）；null = 無募款目標
  amountGoal   Int?
  createdAt    DateTime   @default(now())

  @@index([tab, createdAt(sort: Desc)])
  @@index([categoryCode])
}
