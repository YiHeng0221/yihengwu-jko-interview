# Feature Specs

> One spec per feature. PM produces these from the brief + Figma before any code is written.

## Index

(empty — first spec will be added by `pm` agent during Phase 1)

## Naming

`0NNN-<slug>.md` — three digits, zero-padded.

`pm` agent's job is to produce these from raw requirements. Don't hand-write a spec yourself unless you're playing the PM role explicitly.

## Use `templates/spec-template.md` as the starting point.
